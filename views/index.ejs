<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diseño con Imagen y Fondo Blanco</title>
    <style>
        /* Estilo general del cuerpo */
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            background-color: #007BFF; /* Fondo azul */
            font-family: 'Arial', sans-serif; /* Fuente moderna */
        }

        /* Contenedor de la imagen con fondo blanco */
        .imagen-container {
            background-color: white; /* Fondo blanco */
            padding: 10px; /* Espaciado alrededor de la imagen */
            border-radius: 10px; /* Bordes redondeados */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3); /* Sombra alrededor del contenedor */
            margin-bottom: 20px; /* Espacio entre la imagen y los nombres */
        }

        /* Estilo de la imagen */
        .imagen {
            max-width: 400px; /* Tamaño máximo de la imagen */
            border-radius: 10px; /* Bordes redondeados de la imagen */
        }

        /* Estilo del contenedor de texto */
        .contenedor {
            text-align: center;
            color: white; /* Texto blanco */
            font-size: 3rem; /* Tamaño más grande para los nombres */
            line-height: 4rem; /* Espaciado entre líneas */
            font-weight: bold; /* Texto en negrita */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6); /* Sombra del texto */
        }
    </style>
</head>
<body>
    <!-- Contenedor de la imagen con fondo blanco -->
    <div class="imagen-container">
        <img src="SFS11385.png" alt="Foto decorativa" class="imagen">
    </div>

    <!-- Contenedor de los nombres -->
    <div class="contenedor">
        <p>Rubén Vázquez</p>
        <p>Rubén Mena</p>
        <p>Jose González</p>
    </div>
</body>
</html>
